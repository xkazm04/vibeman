---
phase: 08-self-healing-wiring-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/features/Manager/lib/conductor/conductorOrchestrator.ts
autonomous: true
requirements:
  - HEAL-01
  - HEAL-03
  - HEAL-04
gap_closure: true

must_haves:
  truths:
    - "Error classifications from review stage carry the correct pipelineRunId (not blank)"
    - "Healing patches created by the orchestrator include an expires_at date 7 days in the future"
    - "No dead imports exist in conductorOrchestrator.ts"
  artifacts:
    - path: "src/app/features/Manager/lib/conductor/conductorOrchestrator.ts"
      provides: "Orchestrator with corrected healing wiring"
      contains: "runId"
  key_links:
    - from: "conductorOrchestrator.ts executeReviewStage call"
      to: "reviewStage.ts classifyErrorCanonical"
      via: "runId field on ReviewStageInput"
      pattern: "runId"
    - from: "conductorOrchestrator.ts savePatchToDb"
      to: "conductor_healing_patches.expires_at column"
      via: "INSERT statement with expires_at"
      pattern: "expires_at"
---

<objective>
Fix three integration wiring gaps in conductorOrchestrator.ts identified by the v1.0 milestone audit (INT-01, INT-02, INT-03).

Purpose: Close the last two non-blocking integration issues so error classifications from review stage carry correct runIds (enabling accurate retry-count lookups) and orchestrator-created patches expire by date (enabling full patch lifecycle pruning).

Output: Single file fix — conductorOrchestrator.ts with all three issues resolved.
</objective>

<execution_context>
@C:/Users/kazim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kazim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/app/features/Manager/lib/conductor/review/reviewTypes.ts:
```typescript
export interface ReviewStageInput {
  executionResults: ExecutionResult[];
  currentMetrics: PipelineMetrics;
  currentCycle: number;
  config: BalancingConfig;
  projectId: string;
  projectPath: string;
  specs: SpecMetadata[];
  buildResult: BuildResult;
  goalTitle: string;
  goalDescription: string;
  autoCommit: boolean;
  reviewModel: string | null;
  runId?: string;  // <-- EXISTS but not passed by orchestrator
}
```

From src/app/features/Manager/lib/conductor/selfHealing/promptPatcher.ts:
```typescript
// savePatch includes expires_at (7-day window) — the canonical save function
export async function savePatch(patch: HealingPatch): Promise<void>;
```

Current savePatchToDb in orchestrator (lines 1108-1128) is a LOCAL helper that
omits expires_at from the INSERT. It should be replaced with the canonical
promptPatcher.savePatch() which already includes expires_at.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix runId passthrough and replace savePatchToDb with canonical savePatch</name>
  <files>src/app/features/Manager/lib/conductor/conductorOrchestrator.ts</files>
  <action>
    Three targeted edits in conductorOrchestrator.ts:

    **INT-01 — Add runId to executeReviewStage call (line ~831):**
    Add `runId,` to the object literal passed to `executeReviewStage()`. The `runId` variable is already in scope at the call site (it is the pipeline run ID used throughout the orchestrator). The field already exists as optional on ReviewStageInput. Add it after `reviewModel`:
    ```
    reviewModel: (goalRecord as any)?.review_model || null,
    runId,
    ```

    **INT-02 — Replace savePatchToDb with promptPatcher.savePatch() (lines ~1108-1129):**
    1. Delete the entire `savePatchToDb` function (lines 1108-1129).
    2. Update the import from `./selfHealing/promptPatcher` on line 44 to include `savePatch`:
       Change: `import { buildHealingContext, prunePatches, updatePatchStats } from './selfHealing/promptPatcher';`
       To: `import { buildHealingContext, prunePatches, savePatch, updatePatchStats } from './selfHealing/promptPatcher';`
    3. Replace ALL calls to `savePatchToDb(patch)` (lines ~400 and ~861) with `savePatch(patch)`. Note: savePatch is async but fire-and-forget is acceptable here (the surrounding code does not await the current savePatchToDb either). Use `void savePatch(patch)` to indicate intentional fire-and-forget.

    **INT-03 — Remove dead import (line ~45):**
    Delete line 45: `import { classifyError } from './selfHealing/errorClassifier';`
    This import is unused — the orchestrator uses `classifyErrorCanonical` from reviewStage, not `classifyError` directly.
  </action>
  <verify>
    <automated>cd C:/Users/kazim/dac/vibeman && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
    - executeReviewStage call includes runId field
    - savePatchToDb function deleted, replaced by imported savePatch from promptPatcher
    - Both call sites (line ~400 and ~861) use `void savePatch(patch)` instead of `savePatchToDb(patch)`
    - classifyError import removed
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify wiring correctness with grep assertions</name>
  <files>src/app/features/Manager/lib/conductor/conductorOrchestrator.ts</files>
  <action>
    Run verification checks to confirm all three fixes are correct:

    1. Confirm runId is passed to executeReviewStage:
       `grep -n "runId" conductorOrchestrator.ts` should show runId in the executeReviewStage call object

    2. Confirm savePatchToDb no longer exists:
       `grep -c "savePatchToDb" conductorOrchestrator.ts` should return 0

    3. Confirm savePatch is imported and used:
       `grep "savePatch" conductorOrchestrator.ts` should show the import and `void savePatch(patch)` calls

    4. Confirm classifyError import is gone:
       `grep -c "classifyError" conductorOrchestrator.ts` should return 0

    5. Confirm expires_at coverage by checking promptPatcher.savePatch includes it:
       `grep "expires_at\|expiresAt" selfHealing/promptPatcher.ts` should show the expires_at in the INSERT

    No file modifications in this task — verification only.
  </action>
  <verify>
    <automated>cd C:/Users/kazim/dac/vibeman/src/app/features/Manager/lib/conductor && grep -c "savePatchToDb" conductorOrchestrator.ts | grep "^0$" && grep -c "classifyError" conductorOrchestrator.ts | grep "^0$" && grep "runId," conductorOrchestrator.ts | grep -q "runId" && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <done>
    - savePatchToDb has 0 occurrences in orchestrator
    - classifyError has 0 occurrences in orchestrator
    - runId appears in executeReviewStage call
    - All grep assertions pass
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles cleanly: `npx tsc --noEmit`
2. No references to deleted savePatchToDb function
3. No dead classifyError import
4. runId field present in executeReviewStage call object
5. Existing tests still pass: `npx vitest run tests/api/conductor/ --reporter=verbose`
</verification>

<success_criteria>
- conductorOrchestrator.ts compiles without errors
- INT-01 closed: executeReviewStage receives runId so classifyErrorCanonical in reviewStage.ts can tag error classifications with the correct pipelineRunId
- INT-02 closed: savePatch from promptPatcher replaces savePatchToDb, ensuring all patches get expires_at (7-day window) and initial application_count/success_count fields
- INT-03 closed: dead classifyError import removed
</success_criteria>

<output>
After completion, create `.planning/phases/08-self-healing-wiring-fixes/08-01-SUMMARY.md`
</output>
