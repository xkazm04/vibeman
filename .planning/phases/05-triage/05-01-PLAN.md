---
phase: 05-triage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/features/Manager/lib/conductor/types.ts
  - src/app/db/migrations/204_triage_data_column.ts
  - src/app/db/migrations/index.ts
  - src/lib/brain/conflictDetector.ts
  - tests/api/conductor/triage-checkpoint.test.ts
autonomous: true
requirements: [TRIA-03, BRAIN-03]
must_haves:
  truths:
    - "skipTriage field exists on GoalInput.checkpointConfig type"
    - "triage_data column exists on conductor_runs table"
    - "Brain conflict detector flags items contradicting high-confidence patterns"
    - "Test scaffold exists with describe blocks for all 5 requirements"
  artifacts:
    - path: "src/app/features/Manager/lib/conductor/types.ts"
      provides: "skipTriage on checkpointConfig"
      contains: "skipTriage"
    - path: "src/app/db/migrations/204_triage_data_column.ts"
      provides: "triage_data nullable TEXT column"
      contains: "triage_data"
    - path: "src/lib/brain/conflictDetector.ts"
      provides: "Brain conflict detection for triage items"
      exports: ["detectBrainConflicts"]
    - path: "tests/api/conductor/triage-checkpoint.test.ts"
      provides: "Test scaffold for all triage requirements"
  key_links:
    - from: "src/lib/brain/conflictDetector.ts"
      to: "src/lib/brain/behavioralContext.ts"
      via: "getBehavioralContext import"
      pattern: "getBehavioralContext"
---

<objective>
Add triage types, DB migration, Brain conflict detector, and test scaffold for Phase 5.

Purpose: Establish contracts and infrastructure that Plan 02 (triage stage refactor) builds on.
Output: Updated types.ts, migration 204, conflictDetector.ts, test scaffold.
</objective>

<execution_context>
@C:/Users/kazim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kazim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-triage/05-CONTEXT.md
@.planning/phases/05-triage/05-RESEARCH.md
@src/app/features/Manager/lib/conductor/types.ts
@src/lib/brain/behavioralContext.ts
@src/app/db/migrations/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types, DB migration, and TriageCheckpointData interface</name>
  <files>src/app/features/Manager/lib/conductor/types.ts, src/app/db/migrations/204_triage_data_column.ts, src/app/db/migrations/index.ts</files>
  <action>
1. In `types.ts`, add `skipTriage?: boolean` to the existing `checkpointConfig` shape on `GoalInput` (alongside `preExecute`/`postReview`). Default behavior when absent: false (no skip).

2. Add a new exported interface `TriageCheckpointData`:
```typescript
export interface TriageCheckpointData {
  items: Array<{
    id: string;
    title: string;
    description?: string;
    category: string;
    effort: number | null;
    impact: number | null;
    risk: number | null;
    brainConflict: {
      hasConflict: boolean;
      reason: string | null;
      patternTitle: string | null;
    };
  }>;
  timeoutAt: string; // ISO timestamp
  createdAt: string;
  decisions?: Array<{ itemId: string; action: 'approve' | 'reject' }>;
}
```

3. Add `'triage'` to the checkpoint_type union if one exists in types.

4. Create `src/app/db/migrations/204_triage_data_column.ts` using the `runOnce()` + `addColumnIfNotExists()` pattern from existing migrations (see m202 for reference). Add nullable TEXT column `triage_data` to `conductor_runs`. No NOT NULL, no default.

5. Register migration 204 in `src/app/db/migrations/index.ts` following existing pattern (import + add to `runMigrations()`).
  </action>
  <verify>
    <automated>npx vitest run tests/api/conductor/pipeline.test.ts --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <done>skipTriage on GoalInput type, TriageCheckpointData exported, migration 204 runs without error, existing tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Brain conflict detector and test scaffold</name>
  <files>src/lib/brain/conflictDetector.ts, tests/api/conductor/triage-checkpoint.test.ts</files>
  <action>
1. Create `src/lib/brain/conflictDetector.ts` with:
   - `detectBrainConflicts(items, projectId)` function that returns `Map<string, ConflictResult>`
   - Import `getBehavioralContext` from `./behavioralContext`
   - ConflictResult: `{ hasConflict: boolean; reason: string | null; patternTitle: string | null }`
   - Algorithm: get `topInsights` from behavioral context, filter to `warning` and `pattern_detected` types, for each item compare lowercase title+description against insight description keywords (words > 4 chars), flag if 2+ keyword matches
   - When no insights or no data: return all items with `hasConflict: false`
   - See RESEARCH.md code example for full algorithm reference

2. Create `tests/api/conductor/triage-checkpoint.test.ts` test scaffold:
   - `describe('Triage Checkpoint')` with nested describes:
     - `describe('pauses at triage')` - TRIA-01: test that orchestrator pauses when triage checkpoint is active
     - `describe('approve reject')` - TRIA-02: test batch approve, individual reject, mixed decisions
     - `describe('skipTriage')` - TRIA-03: test bypass when skipTriage=true
     - `describe('timeout')` - TRIA-04: test timeout interrupts pipeline
     - `describe('brain conflict')` - BRAIN-03: test conflict detection flags contradictions, no-data returns clean
   - Each describe should have 1-2 test stubs (`it.todo(...)`) describing what will be tested in Plan 03
   - Add concrete tests for `detectBrainConflicts`:
     - Test with mock items + mock `getBehavioralContext` returning warning insights -> items flagged
     - Test with no insights -> no items flagged
     - Test with low-match items -> no flag
   - Mock `getBehavioralContext` using `vi.mock('../../src/lib/brain/behavioralContext')`
  </action>
  <verify>
    <automated>npx vitest run tests/api/conductor/triage-checkpoint.test.ts --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <done>conflictDetector exports detectBrainConflicts, test file has describe blocks for all 5 requirements, Brain conflict unit tests pass</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/api/conductor/triage-checkpoint.test.ts` -- brain conflict tests pass
- `npx vitest run tests/api/conductor/pipeline.test.ts` -- existing tests unbroken
- `grep -r "skipTriage" src/app/features/Manager/lib/conductor/types.ts` confirms type exists
- `grep -r "triage_data" src/app/db/migrations/204_triage_data_column.ts` confirms migration
</verification>

<success_criteria>
- TriageCheckpointData and skipTriage types exported from types.ts
- Migration 204 adds triage_data column without breaking existing data
- detectBrainConflicts correctly flags items matching warning insights
- Test scaffold covers all 5 phase requirements with describe blocks
</success_criteria>

<output>
After completion, create `.planning/phases/05-triage/05-01-SUMMARY.md`
</output>
