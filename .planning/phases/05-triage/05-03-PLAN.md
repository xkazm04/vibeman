---
phase: 05-triage
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - tests/api/conductor/triage-checkpoint.test.ts
autonomous: true
requirements: [TRIA-01, TRIA-02, TRIA-03, TRIA-04, BRAIN-03]
must_haves:
  truths:
    - "Tests prove pipeline pauses at triage and resumes on user decision"
    - "Tests prove batch approve/reject decisions are applied correctly"
    - "Tests prove skipTriage bypasses checkpoint"
    - "Tests prove timeout interrupts pipeline cleanly"
    - "Tests prove Brain conflict detection flags high-confidence contradictions"
  artifacts:
    - path: "tests/api/conductor/triage-checkpoint.test.ts"
      provides: "Integration tests for all 5 triage requirements"
      min_lines: 100
  key_links:
    - from: "tests/api/conductor/triage-checkpoint.test.ts"
      to: "src/app/features/Manager/lib/conductor/conductorOrchestrator.ts"
      via: "imports orchestrator or calls API routes"
      pattern: "triage|waitForResume"
---

<objective>
Complete integration tests for triage checkpoint covering all 5 phase requirements.

Purpose: Prove that triage pause, approve/reject, bypass, timeout, and Brain conflict detection all work correctly.
Output: Comprehensive test file with passing tests for TRIA-01 through TRIA-04 and BRAIN-03.
</objective>

<execution_context>
@C:/Users/kazim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kazim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-triage/05-CONTEXT.md
@.planning/phases/05-triage/05-RESEARCH.md
@.planning/phases/05-triage/05-01-SUMMARY.md
@.planning/phases/05-triage/05-02-SUMMARY.md
@tests/api/conductor/triage-checkpoint.test.ts
@tests/api/conductor/pipeline.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration tests for triage checkpoint flow</name>
  <files>tests/api/conductor/triage-checkpoint.test.ts</files>
  <action>
Replace the todo stubs from Plan 01 with full integration tests. Follow the patterns established in `pipeline.test.ts` for DB setup, mocking, and assertion style.

**Test setup:**
- Use test DB with migrations applied (including migration 204)
- Mock `getBehavioralContext` to return controlled topInsights
- Mock tinder API calls to verify approve/reject actions
- Create helper to insert test conductor_run rows with known IDs

**TRIA-01 tests (pauses at triage):**
- Test: when triage checkpoint is active, orchestrator writes 'paused' status + checkpoint_type='triage' + triage_data JSON to conductor_runs
- Test: triage_data contains items with scores and brainConflict fields
- Test: triage_data contains timeoutAt timestamp ~1 hour in the future

**TRIA-02 tests (approve/reject):**
- Test: POST /api/conductor/triage with all-approve decisions -> all items approved via tinder API, run status set to 'running'
- Test: POST /api/conductor/triage with mixed decisions -> only approved items sent to tinder API accept, rejected items sent to reject
- Test: POST /api/conductor/triage when run is NOT paused -> returns 409
- Test: POST /api/conductor/triage when checkpoint_type is not 'triage' -> returns 409

**TRIA-03 tests (skipTriage bypass):**
- Test: when checkpointConfig.skipTriage=true, triage scoring runs but no pause occurs, all items auto-approved
- Test: when skipTriage is absent/undefined, defaults to false (checkpoint active)

**TRIA-04 tests (timeout):**
- Test: waitForResumeWithTimeout returns 'timeout' when timeoutMs elapses (use small timeout like 100ms for test speed)
- Test: on timeout, run status is set to 'interrupted' with reason logged
- Test: on timeout, triage_data is cleaned up

**BRAIN-03 tests (brain conflict):**
- Keep and expand the unit tests from Plan 01
- Test: items matching warning insight keywords get hasConflict=true with patternTitle
- Test: items not matching get hasConflict=false
- Test: when getBehavioralContext returns empty/no data, all items clean
- Test: only warning and pattern_detected insight types trigger conflicts (not preference_learned)
  </action>
  <verify>
    <automated>npx vitest run tests/api/conductor/triage-checkpoint.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All tests pass covering TRIA-01 (pause), TRIA-02 (approve/reject + 409 race), TRIA-03 (skipTriage), TRIA-04 (timeout + cleanup), BRAIN-03 (conflict detection)</done>
</task>

<task type="auto">
  <name>Task 2: Full suite validation</name>
  <files>tests/api/conductor/triage-checkpoint.test.ts</files>
  <action>
Run the full test suite to confirm no regressions from Phase 5 changes:
1. Run `npx vitest run` -- all existing tests must pass alongside new triage tests
2. If any existing test breaks due to type changes (skipTriage on GoalInput), fix the test setup to include the new optional field
3. Run `npx tsc --noEmit` to confirm zero type errors across the whole project
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <done>Full test suite passes, tsc --noEmit clean, no regressions from Phase 5</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/api/conductor/triage-checkpoint.test.ts` -- all triage tests pass
- `npx vitest run` -- full suite green
- `npx tsc --noEmit` -- zero type errors
</verification>

<success_criteria>
- At least 10 tests covering all 5 requirement IDs
- All tests pass
- Full suite has no regressions
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/05-triage/05-03-SUMMARY.md`
</output>
