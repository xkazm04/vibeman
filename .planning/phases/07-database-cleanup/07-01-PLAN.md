---
phase: 07-database-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/db/repositories/hypothesis-testing.repository.ts
  - src/app/db/repositories/developer-mind-meld.repository.ts
  - src/app/db/repositories/red-team.repository.ts
  - src/app/db/repositories/focus-mode.repository.ts
  - src/app/db/repositories/adaptive-learning.repository.ts
  - src/app/db/index.ts
  - src/app/db/migrations/064_drop_orphaned_tables.ts
  - src/app/db/migrations/index.ts
  - src/app/db/models/hypothesis-testing.types.ts
  - src/app/db/models/red-team.types.ts
  - src/app/db/models/focus-mode.types.ts
autonomous: true

must_haves:
  truths:
    - "All 5 orphaned repository files are deleted"
    - "Database index has no imports or exports for deleted repositories"
    - "Migration exists to drop 21 orphaned tables"
    - "TypeScript compilation passes with no errors"
    - "Related type files are deleted or cleaned up"
  artifacts:
    - path: "src/app/db/migrations/064_drop_orphaned_tables.ts"
      provides: "Migration to drop 21 orphaned database tables"
      contains: "DROP TABLE IF EXISTS"
  key_links:
    - from: "src/app/db/index.ts"
      to: "repositories/*"
      via: "import statements"
      pattern: "Repository imports must not reference deleted files"
    - from: "src/app/db/migrations/index.ts"
      to: "src/app/db/migrations/064_drop_orphaned_tables.ts"
      via: "runMigrations() call"
      pattern: "migrateDropOrphanedTables"
---

<objective>
Remove 5 orphaned database repository files, clean up the database index exports, and create a migration to drop 21 unused tables from the database.

Purpose: Complete the database layer cleanup of v1.1 Dead Code Cleanup, removing approximately 4,351 lines of unused repository code and preparing the database for table cleanup.

Output:
- 5 repository files deleted
- 3 type files deleted
- Database index cleaned (23 exports removed)
- Migration file created to drop 21 tables
</objective>

<execution_context>
@C:\Users\kazda\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\kazda\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/db/index.ts
@src/app/db/migrations/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete orphaned repository and type files</name>
  <files>
    src/app/db/repositories/hypothesis-testing.repository.ts
    src/app/db/repositories/developer-mind-meld.repository.ts
    src/app/db/repositories/red-team.repository.ts
    src/app/db/repositories/focus-mode.repository.ts
    src/app/db/repositories/adaptive-learning.repository.ts
    src/app/db/models/hypothesis-testing.types.ts
    src/app/db/models/red-team.types.ts
    src/app/db/models/focus-mode.types.ts
  </files>
  <action>
Delete the following 8 files (5 repositories + 3 type files):

**Repository files to delete:**
1. `src/app/db/repositories/hypothesis-testing.repository.ts` (1112 lines)
2. `src/app/db/repositories/developer-mind-meld.repository.ts` (939 lines)
3. `src/app/db/repositories/red-team.repository.ts` (1051 lines)
4. `src/app/db/repositories/focus-mode.repository.ts` (761 lines)
5. `src/app/db/repositories/adaptive-learning.repository.ts` (488 lines)

**Type files to delete:**
6. `src/app/db/models/hypothesis-testing.types.ts`
7. `src/app/db/models/red-team.types.ts`
8. `src/app/db/models/focus-mode.types.ts`

Use `rm` command or equivalent to delete each file. Verify deletion with `ls`.

Note: Do NOT delete `src/app/db/models/types.ts` - it contains shared types including `DbIdeaExecutionOutcome`, `DbScoringWeight`, `DbScoringThreshold`, etc. that may be used by other parts of the system. We will clean those up in a later verification pass if needed.
  </action>
  <verify>
Run `ls src/app/db/repositories/*.ts | grep -E "(hypothesis|mind-meld|red-team|focus-mode|adaptive-learning)" | wc -l` returns 0.
Run `ls src/app/db/models/*.ts | grep -E "(hypothesis-testing|red-team|focus-mode)" | wc -l` returns 0.
  </verify>
  <done>All 8 files deleted (5 repository files + 3 type files)</done>
</task>

<task type="auto">
  <name>Task 2: Clean up database index exports</name>
  <files>src/app/db/index.ts</files>
  <action>
Edit `src/app/db/index.ts` to remove all imports and exports related to the deleted repositories.

**Remove these import blocks:**

1. Lines importing from `adaptive-learning.repository.ts`:
```typescript
import {
  ideaExecutionOutcomeRepository,
  scoringWeightRepository,
  scoringThresholdRepository,
} from './repositories/adaptive-learning.repository';
```

2. Lines importing from `developer-mind-meld.repository.ts`:
```typescript
import {
  developerProfileRepository,
  developerDecisionRepository,
  learningInsightRepository,
  codePatternUsageRepository,
  consistencyRuleRepository,
  skillTrackingRepository,
} from './repositories/developer-mind-meld.repository';
```

3. Lines importing from `hypothesis-testing.repository.ts`:
```typescript
import {
  hypothesisRepository,
  invariantRepository,
  fuzzSessionRepository,
  propertyTestRepository,
  testKnowledgeRepository,
  hypothesisTestingSummaryRepository,
} from './repositories/hypothesis-testing.repository';
```

4. Lines importing from `red-team.repository.ts`:
```typescript
import {
  redTeamSessionRepository,
  redTeamAttackRepository,
  redTeamVulnerabilityRepository,
  vulnerabilityDebateRepository,
  redTeamSummaryRepository,
} from './repositories/red-team.repository';
```

5. Lines importing from `focus-mode.repository.ts`:
```typescript
import {
  focusSessionRepository,
  focusBreakRepository,
  focusStatsRepository,
} from './repositories/focus-mode.repository';
```

**Remove these type exports:**
```typescript
export * from './models/red-team.types';
export * from './models/focus-mode.types';
```

Note: The comment `// NOTE: hypothesis-testing.types removed` already exists, so no change needed there.

**Remove these database export objects (23 exports total):**

Adaptive Learning (3 exports):
- `ideaExecutionOutcomeDb`
- `scoringWeightDb`
- `scoringThresholdDb`

Developer Mind-Meld (6 exports):
- `developerProfileDb`
- `developerDecisionDb`
- `learningInsightDb`
- `codePatternUsageDb`
- `consistencyRuleDb`
- `skillTrackingDb`

Hypothesis Testing (6 exports):
- `hypothesisDb`
- `invariantDb`
- `fuzzSessionDb`
- `propertyTestDb`
- `testKnowledgeDb`
- `hypothesisTestingSummaryDb`

Red Team (5 exports):
- `redTeamSessionDb`
- `redTeamAttackDb`
- `redTeamVulnerabilityDb`
- `vulnerabilityDebateDb`
- `redTeamSummaryDb`

Focus Mode (3 exports):
- `focusSessionDb`
- `focusBreakDb`
- `focusStatsDb`

Read the file first, then remove all the listed imports and exports while preserving the rest of the file structure.
  </action>
  <verify>
Run `npx tsc --noEmit src/app/db/index.ts 2>&1 | head -20` - should show no errors related to missing modules.
Run `grep -c "adaptive-learning.repository" src/app/db/index.ts` returns 0.
Run `grep -c "developer-mind-meld.repository" src/app/db/index.ts` returns 0.
Run `grep -c "hypothesis-testing.repository" src/app/db/index.ts` returns 0.
Run `grep -c "red-team.repository" src/app/db/index.ts` returns 0.
Run `grep -c "focus-mode.repository" src/app/db/index.ts` returns 0.
  </verify>
  <done>Database index cleaned - 5 import blocks and 23 export objects removed</done>
</task>

<task type="auto">
  <name>Task 3: Create migration to drop orphaned tables</name>
  <files>
    src/app/db/migrations/064_drop_orphaned_tables.ts
    src/app/db/migrations/index.ts
  </files>
  <action>
Create a new migration file `src/app/db/migrations/064_drop_orphaned_tables.ts` that drops the 21 orphaned tables.

**Tables to drop (21 total):**

Hypothesis Testing (5 tables):
- `hypotheses`
- `invariants`
- `fuzz_sessions`
- `property_tests`
- `test_knowledge`

Developer Mind-Meld (6 tables):
- `developer_profiles`
- `developer_decisions`
- `learning_insights`
- `code_pattern_usage`
- `consistency_rules`
- `skill_tracking`

Red Team (4 tables):
- `red_team_sessions`
- `red_team_attacks`
- `red_team_vulnerabilities`
- `vulnerability_debates`

Focus Mode (3 tables):
- `focus_sessions`
- `focus_breaks`
- `focus_stats`

Adaptive Learning (3 tables):
- `idea_execution_outcomes`
- `scoring_weights`
- `scoring_thresholds`

**Migration file content pattern:**
```typescript
/**
 * Migration 064: Drop Orphaned Tables
 *
 * Removes 21 tables from deprecated features:
 * - Hypothesis Testing (5 tables)
 * - Developer Mind-Meld (6 tables)
 * - Red Team (4 tables)
 * - Focus Mode (3 tables)
 * - Adaptive Learning (3 tables)
 *
 * These features were removed in v1.1 Dead Code Cleanup.
 */
import { getConnection } from '../drivers';

export function migrateDropOrphanedTables() {
  const db = getConnection();

  const tablesToDrop = [
    // Hypothesis Testing
    'hypotheses',
    'invariants',
    'fuzz_sessions',
    'property_tests',
    'test_knowledge',
    // Developer Mind-Meld
    'developer_profiles',
    'developer_decisions',
    'learning_insights',
    'code_pattern_usage',
    'consistency_rules',
    'skill_tracking',
    // Red Team
    'red_team_sessions',
    'red_team_attacks',
    'red_team_vulnerabilities',
    'vulnerability_debates',
    // Focus Mode
    'focus_sessions',
    'focus_breaks',
    'focus_stats',
    // Adaptive Learning
    'idea_execution_outcomes',
    'scoring_weights',
    'scoring_thresholds',
  ];

  for (const table of tablesToDrop) {
    try {
      db.exec(`DROP TABLE IF EXISTS ${table}`);
    } catch (error) {
      // Table may not exist, that's fine
    }
  }
}
```

**Update migrations/index.ts:**
1. Add import at the top: `import { migrateDropOrphanedTables } from './064_drop_orphaned_tables';`
2. Add migration call at the end of runMigrations(): `migrateDropOrphanedTables();`
3. Add appropriate comment for the migration number.

Look at the existing migration pattern in index.ts to match the style (numbered migrations with comments).
  </action>
  <verify>
Run `ls src/app/db/migrations/064_drop_orphaned_tables.ts` - file should exist.
Run `grep -c "migrateDropOrphanedTables" src/app/db/migrations/index.ts` returns at least 2 (import + call).
Run `npx tsc --noEmit src/app/db/migrations/064_drop_orphaned_tables.ts 2>&1` - should have no errors.
  </verify>
  <done>Migration file created with 21 DROP TABLE statements, integrated into migrations/index.ts</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **File deletion verification:**
   ```bash
   ls src/app/db/repositories/ | grep -E "(hypothesis|mind-meld|red-team|focus-mode|adaptive-learning)"
   # Should return nothing
   ```

2. **Index cleanup verification:**
   ```bash
   # Verify no references to deleted repository files remain
   grep -c "adaptive-learning.repository" src/app/db/index.ts
   grep -c "developer-mind-meld.repository" src/app/db/index.ts
   grep -c "hypothesis-testing.repository" src/app/db/index.ts
   grep -c "red-team.repository" src/app/db/index.ts
   grep -c "focus-mode.repository" src/app/db/index.ts
   # All should return 0
   ```

3. **Migration verification:**
   ```bash
   grep "DROP TABLE IF EXISTS" src/app/db/migrations/064_drop_orphaned_tables.ts | wc -l
   # Should return 21
   ```

4. **TypeScript compilation:**
   ```bash
   npx tsc --noEmit
   # Should pass with no errors related to database layer
   ```
</verification>

<success_criteria>
- [ ] 5 repository files deleted (~4,351 lines removed)
- [ ] 3 type files deleted (hypothesis-testing.types.ts, red-team.types.ts, focus-mode.types.ts)
- [ ] Database index cleaned (5 import blocks, 2 type exports, 23 database exports removed)
- [ ] Migration file `064_drop_orphaned_tables.ts` created with 21 DROP TABLE statements
- [ ] Migration integrated into `migrations/index.ts`
- [ ] `npx tsc --noEmit` passes with no errors in database layer
</success_criteria>

<output>
After completion, create `.planning/phases/07-database-cleanup/07-01-SUMMARY.md`
</output>
