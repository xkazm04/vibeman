---
phase: 06-server-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/codeGenerationDatabase.ts (DELETE)
  - src/lib/backlogDatabase.ts (DELETE)
  - src/lib/claudeTaskManager.ts (DELETE)
  - src/lib/impactedFilesUtils.ts (DELETE)
  - src/app/api/project/context/route.ts (DELETE)
autonomous: true

must_haves:
  truths:
    - "codeGenerationDatabase.ts no longer exists in filesystem"
    - "backlogDatabase.ts no longer exists in filesystem"
    - "claudeTaskManager.ts no longer exists in filesystem"
    - "impactedFilesUtils.ts no longer exists in filesystem"
    - "/api/project/context route no longer exists in filesystem"
    - "TypeScript compilation passes with zero errors"
  artifacts: []
  key_links: []
---

<objective>
Delete 5 unused server-side files totaling ~860 lines of dead code.

Purpose: Continue v1.1 dead code cleanup by removing unused lib utilities and API routes from the server layer.
Output: 5 files deleted, TypeScript compilation verified.
</objective>

<execution_context>
@C:\Users\kazda\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\kazda\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete unused lib files</name>
  <files>
    src/lib/codeGenerationDatabase.ts
    src/lib/backlogDatabase.ts
    src/lib/claudeTaskManager.ts
    src/lib/impactedFilesUtils.ts
  </files>
  <action>
    Delete the following 4 files from src/lib/:

    1. codeGenerationDatabase.ts (394 lines) - Unused database utility
    2. backlogDatabase.ts (285 lines) - Unused backlog database helper
    3. claudeTaskManager.ts (85 lines) - Unused task manager
    4. impactedFilesUtils.ts (52 lines) - Unused file impact utility

    Use rm or file deletion. These files have been confirmed unused through prior dead code analysis.
  </action>
  <verify>
    ls src/lib/codeGenerationDatabase.ts 2>&1 | grep -q "No such file"
    ls src/lib/backlogDatabase.ts 2>&1 | grep -q "No such file"
    ls src/lib/claudeTaskManager.ts 2>&1 | grep -q "No such file"
    ls src/lib/impactedFilesUtils.ts 2>&1 | grep -q "No such file"
  </verify>
  <done>All 4 lib files deleted from filesystem</done>
</task>

<task type="auto">
  <name>Task 2: Delete unused API route and verify compilation</name>
  <files>
    src/app/api/project/context/route.ts
  </files>
  <action>
    1. Delete the unused API route:
       - src/app/api/project/context/route.ts (44 lines)

    2. After deletion, run TypeScript type checking:
       npx tsc --noEmit

    3. If tsc passes with zero errors, the phase is complete.

    4. If tsc fails with import errors referencing deleted files:
       - The error indicates another file imports the deleted file
       - This would be unexpected (files confirmed unused)
       - Report the error but do not restore deleted files
  </action>
  <verify>
    ls src/app/api/project/context/route.ts 2>&1 | grep -q "No such file"
    npx tsc --noEmit exits with code 0
  </verify>
  <done>API route deleted and TypeScript compilation passes with zero errors</done>
</task>

</tasks>

<verification>
1. Confirm all 5 files no longer exist:
   - [ ] src/lib/codeGenerationDatabase.ts
   - [ ] src/lib/backlogDatabase.ts
   - [ ] src/lib/claudeTaskManager.ts
   - [ ] src/lib/impactedFilesUtils.ts
   - [ ] src/app/api/project/context/route.ts

2. TypeScript compilation:
   - [ ] `npx tsc --noEmit` passes with zero errors
</verification>

<success_criteria>
- 5 files deleted (~860 lines removed)
- No TypeScript compilation errors
- Requirements SERVER-01 through SERVER-05 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/06-server-cleanup/06-01-SUMMARY.md`
</output>
