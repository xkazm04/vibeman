---
phase: 08-feature-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/features/Context/ContextMenu/FileTreeSelector.tsx
  - src/app/features/Context/components/tree/TreeView.tsx
  - src/app/features/Context/components/tree/TreeNode.tsx
  - src/app/features/Context/components/tree/CodePreviewModal.tsx
  - src/app/features/Context/components/tree/FileErrorDisplay.tsx
  - src/app/features/Context/components/tree/fileOperationErrors.ts
autonomous: true

must_haves:
  truths:
    - "CodeTree directory no longer exists"
    - "RefactorSuggestion directory no longer exists"
    - "ScanQueue feature components directory no longer exists"
    - "FileTreeSelector still renders tree views correctly"
    - "TypeScript compilation passes with no errors"
  artifacts:
    - path: "src/app/features/Context/components/tree/TreeView.tsx"
      provides: "Tree view component for file selection"
    - path: "src/app/features/Context/components/tree/TreeNode.tsx"
      provides: "Individual tree node with expand/collapse"
    - path: "src/app/features/Context/components/tree/CodePreviewModal.tsx"
      provides: "File preview modal on right-click"
  key_links:
    - from: "src/app/features/Context/ContextMenu/FileTreeSelector.tsx"
      to: "src/app/features/Context/components/tree/TreeView.tsx"
      via: "relative import"
      pattern: "from ['\"]\\.\\./components/tree/TreeView"
---

<objective>
Delete abandoned feature modules (CodeTree, RefactorSuggestion, ScanQueue) totaling ~4,833 lines.

Purpose: Remove dead code to reduce maintenance burden and improve codebase clarity. This is part of the v1.1 Dead Code Cleanup milestone.

Output: Three feature directories deleted with no import errors. Used components relocated to Context feature.
</objective>

<execution_context>
@C:\Users\kazda\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\kazda\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-database-cleanup/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate used CodeTree components to Context feature</name>
  <files>
    src/app/features/Context/components/tree/TreeView.tsx
    src/app/features/Context/components/tree/TreeNode.tsx
    src/app/features/Context/components/tree/CodePreviewModal.tsx
    src/app/features/Context/components/tree/FileErrorDisplay.tsx
    src/app/features/Context/components/tree/fileOperationErrors.ts
    src/app/features/Context/ContextMenu/FileTreeSelector.tsx
  </files>
  <action>
    FileTreeSelector.tsx imports TreeView from CodeTree. Before deleting CodeTree, relocate the actively-used components.

    1. Create directory: `src/app/features/Context/components/tree/`

    2. Copy these files from CodeTree to the new tree directory:
       - `TreeView.tsx` -> `src/app/features/Context/components/tree/TreeView.tsx`
       - `TreeNode.tsx` -> `src/app/features/Context/components/tree/TreeNode.tsx`
       - `CodePreviewModal.tsx` -> `src/app/features/Context/components/tree/CodePreviewModal.tsx`
       - `components/FileErrorDisplay.tsx` -> `src/app/features/Context/components/tree/FileErrorDisplay.tsx`
       - `lib/fileOperationErrors.ts` -> `src/app/features/Context/components/tree/fileOperationErrors.ts`

    3. Update imports in the copied files:
       - TreeView.tsx: Change `import TreeNode from './TreeNode'` (already relative, should work)
       - TreeNode.tsx: Update path to CodePreviewModal (from `./CodePreviewModal` - already relative)
       - CodePreviewModal.tsx: Update import of FileErrorDisplay from `./components/FileErrorDisplay` to `./FileErrorDisplay`
       - CodePreviewModal.tsx: Update import of fileOperationErrors from `./lib/fileOperationErrors` to `./fileOperationErrors`

    4. Update FileTreeSelector.tsx import:
       - Change: `import TreeView from '../../CodeTree/TreeView';`
       - To: `import TreeView from '../components/tree/TreeView';`

    Note: TreeNode imports from stores and utils (`../../../types`, `../../../stores/nodeStore`, etc.) - these paths should still work since we're at a similar depth.
  </action>
  <verify>
    Run `npx tsc --noEmit` - should pass with no errors from the relocated tree components
  </verify>
  <done>
    - Tree components relocated to Context/components/tree/
    - FileTreeSelector imports from new location
    - TypeScript compilation passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete abandoned feature directories</name>
  <files>
    src/app/features/CodeTree/ (entire directory - 17 files)
    src/app/features/RefactorSuggestion/ (entire directory - 6 files)
    src/app/features/ScanQueue/ (entire directory - 4 files)
  </files>
  <action>
    Delete the three feature directories that are no longer used:

    1. Delete `src/app/features/CodeTree/` directory (entire directory)
       - 17 files, ~2,411 lines
       - Used components already relocated in Task 1
       - Remaining files are dead code: TreeSearch, TreeSearchInput, TreeHeader, TreeFooter, TreeSuggestionsDropdown, TreeSuggestion, lib/* (projectApi, hooks, searchRankingStrategy, treeUtils, index)

    2. Delete `src/app/features/RefactorSuggestion/` directory (entire directory)
       - 6 files, ~1,463 lines
       - No external imports (verified via grep)
       - Contains: RefactorSuggestionPanel, refactorSuggestionEngine, ideaGenerator, index files

    3. Delete `src/app/features/ScanQueue/` directory (entire directory)
       - 4 files, ~959 lines
       - No external imports (verified via grep)
       - Note: This is the UI components only - scanQueue.repository.ts, scanQueueWorker.ts, and /api/scan-queue routes are KEPT (they're active database/API layer)
       - Contains: ScanNotifications, ScanQueueControl, ScanQueueProgress, DraggableQueue

    Use: `rm -rf` on each directory (or equivalent Windows command)
  </action>
  <verify>
    1. Verify directories deleted:
       - `ls src/app/features/CodeTree` should fail (directory not found)
       - `ls src/app/features/RefactorSuggestion` should fail
       - `ls src/app/features/ScanQueue` should fail

    2. Run `npx tsc --noEmit` - should pass with no import errors

    3. Verify ScanQueue backend is intact:
       - `ls src/app/db/repositories/scanQueue.repository.ts` should exist
       - `ls src/lib/scanQueueWorker.ts` should exist
  </verify>
  <done>
    - src/app/features/CodeTree/ deleted (17 files, ~2,411 lines)
    - src/app/features/RefactorSuggestion/ deleted (6 files, ~1,463 lines)
    - src/app/features/ScanQueue/ deleted (4 files, ~959 lines)
    - Total: ~4,833 lines of dead code removed
    - No import errors in codebase
    - ScanQueue backend (repository, worker, API) remains intact
  </done>
</task>

</tasks>

<verification>
1. TypeScript compilation: `npx tsc --noEmit` passes with zero errors
2. Feature directories deleted:
   - CodeTree directory does not exist
   - RefactorSuggestion directory does not exist
   - ScanQueue directory does not exist
3. FileTreeSelector works: imports TreeView from relocated path
4. ScanQueue backend intact: repository, worker, and API routes still exist
</verification>

<success_criteria>
- `src/app/features/CodeTree/` directory deleted
- `src/app/features/RefactorSuggestion/` directory deleted
- `src/app/features/ScanQueue/` directory deleted
- No TypeScript import errors anywhere in codebase
- ~4,833 lines of dead code removed
- FileTreeSelector still functional with relocated tree components
</success_criteria>

<output>
After completion, create `.planning/phases/08-feature-cleanup/08-01-SUMMARY.md`
</output>
