---
phase: 09-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false

must_haves:
  truths:
    - "TypeScript compilation passes with zero errors"
    - "Production build completes successfully"
    - "Development server starts without errors"
    - "Core features are accessible without runtime errors"
  artifacts:
    - path: ".next/"
      provides: "Production build output"
  key_links:
    - from: "All source files"
      to: "tsconfig.json"
      via: "TypeScript compilation"
      pattern: "npx tsc --noEmit"
    - from: "All source files"
      to: ".next/ build output"
      via: "Next.js build"
      pattern: "npm run build"
---

<objective>
Verify the codebase builds, types check, and application runs correctly after the v1.1 dead code cleanup.

Purpose: Confirm that the deletion of ~10,000 lines across 4 cleanup phases (client, server, database, feature) has not broken the application. This is the final verification gate before v1.1 completion.

Output: Verified working codebase with passing type checks, successful build, and functioning application.
</objective>

<execution_context>
@C:\Users\kazda\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\kazda\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run TypeScript type check</name>
  <files>None modified - verification only</files>
  <action>
Run TypeScript compiler in check mode:

```bash
npx tsc --noEmit
```

**Expected result:** Zero errors, zero warnings related to missing imports or type errors.

**If errors occur:**
1. Analyze the error messages to identify broken imports or missing types
2. Fix import statements by either:
   - Removing imports of deleted modules
   - Updating import paths for relocated modules
3. Re-run type check until it passes

**Common issues to watch for:**
- Imports from deleted stores (focusModeStore, terminalStore)
- Imports from deleted hooks (useScanData, useBacklogQuery, useSmoothNavigation)
- Imports from deleted features (CodeTree, RefactorSuggestion, ScanQueue)
- Imports from deleted repositories (hypothesis-testing, developer-mind-meld, etc.)

Do NOT suppress errors with @ts-ignore. Fix the underlying issue.
  </action>
  <verify>
Run `npx tsc --noEmit` and confirm output shows no errors. Output should be empty or show only "Done" message.
  </verify>
  <done>
TypeScript compilation completes with zero errors. VERIFY-01 satisfied.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run production build</name>
  <files>None modified - verification only</files>
  <action>
Run Next.js production build:

```bash
npm run build
```

**Expected result:** Build completes successfully with output showing pages compiled.

**If build fails:**
1. Analyze build errors (often more revealing than tsc errors)
2. Common issues:
   - Dynamic imports of deleted modules
   - Server components importing deleted client code
   - Missing environment variables (unlikely for cleanup)
3. Fix issues and re-run build

**Success indicators:**
- "Compiled successfully" message
- Route manifest shows expected pages
- No "Module not found" errors
  </action>
  <verify>
Run `npm run build` and confirm it completes with "Compiled successfully" or equivalent success message. Check that build output in terminal shows no errors.
  </verify>
  <done>
Production build completes successfully without errors. VERIFY-02 satisfied.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Completed TypeScript check and production build verification. The codebase compiles without type errors and builds successfully for production.
  </what-built>
  <how-to-verify>
1. Start the development server: `npm run dev`
2. Open browser to http://localhost:3000
3. Navigate through core features:
   - Home/Dashboard page loads
   - Ideas feature accessible
   - Contexts feature accessible
   - Goals feature accessible
   - Task Runner accessible
   - Settings accessible
4. Verify no console errors (open DevTools > Console)
5. Verify no runtime crashes on navigation

**What to look for:**
- Pages load without blank screens
- No "Module not found" errors in console
- No React hydration errors
- Navigation between features works
  </how-to-verify>
  <resume-signal>Type "approved" if application runs correctly, or describe any issues encountered.</resume-signal>
</task>

</tasks>

<verification>
Phase verification checklist:
- [ ] `npx tsc --noEmit` passes with zero errors
- [ ] `npm run build` completes successfully
- [ ] `npm run dev` starts without errors
- [ ] Core features accessible in browser
- [ ] No runtime errors in browser console
</verification>

<success_criteria>
1. TypeScript compilation: `npx tsc --noEmit` exits with code 0, no errors
2. Production build: `npm run build` completes with "Compiled successfully"
3. Dev server: `npm run dev` starts and serves application
4. Runtime: User confirms core features work without errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-verification/09-01-SUMMARY.md`
</output>
