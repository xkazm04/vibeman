---
phase: 03-execute-stage
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - tests/conductor/execute-stage.test.ts
  - tests/conductor/checkpoints.test.ts
autonomous: true
requirements: [EXEC-01, EXEC-02, EXEC-03, EXEC-04, VALD-01]

must_haves:
  truths:
    - "Tests prove overlapping specs are serialized, not concurrent"
    - "Tests prove exit-0 with no file changes is marked failed"
    - "Tests prove spec status transitions through pending -> executing -> completed/failed"
    - "Tests prove checkpoint config controls pause behavior"
    - "Tests prove build validation result is stored on run record"
    - "Full conductor test suite passes"
  artifacts:
    - path: "tests/conductor/execute-stage.test.ts"
      provides: "Integration tests for refactored execute stage"
      min_lines: 80
    - path: "tests/conductor/checkpoints.test.ts"
      provides: "Tests for checkpoint pause/resume and build validation"
      min_lines: 60
  key_links:
    - from: "tests/conductor/execute-stage.test.ts"
      to: "stages/executeStage.ts"
      via: "imports executeExecuteStage"
      pattern: "import.*executeExecuteStage"
    - from: "tests/conductor/checkpoints.test.ts"
      to: "conductorOrchestrator.ts"
      via: "tests checkpoint and build validation behavior"
      pattern: "checkpoint|build.?validation"
---

<objective>
Write integration tests validating the refactored execute stage, checkpoint behavior, and build validation. These tests exercise the full Phase 3 behavioral surface.

Purpose: Prove all five requirements (EXEC-01 through EXEC-04, VALD-01) work correctly by testing the integrated modules from Plans 01 and 02.

Output: Two test files covering domain isolation, file verification, spec status tracking, checkpoint pauses, and build validation.
</objective>

<execution_context>
@C:/Users/kazim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kazim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-execute-stage/03-CONTEXT.md
@.planning/phases/03-execute-stage/03-01-SUMMARY.md
@.planning/phases/03-execute-stage/03-02-SUMMARY.md

<interfaces>
<!-- From Plan 02 outputs — executor needs these contracts for test setup -->

From stages/executeStage.ts (refactored in 03-02):
```typescript
interface ExecuteInput {
  runId: string;
  config: BalancingConfig;
  projectId: string;
  projectPath: string;
  projectName: string;
  abortSignal?: AbortSignal;
  onTaskUpdate?: (tasks: ExecutionTaskState[]) => void;
}
export async function executeExecuteStage(input: ExecuteInput): Promise<ExecuteStageResult>;
```

From spec/specRepository.ts (existing):
```typescript
export const specRepository = {
  createSpec(params): SpecMetadata;
  getSpecsByRunId(runId: string): SpecMetadata[];
  updateSpecStatus(specId: string, status): void;
};
```

From conductor.repository.ts (existing):
```typescript
export const conductorRepository = {
  createRun(params): DbPipelineRun;
  getRunById(runId: string): DbPipelineRun | null;
  updateRunStatus(runId: string, status: string): void;
};
```

Existing test pattern (from tests/conductor/spec-writer.test.ts):
```typescript
// Tests use real SQLite DB via getDatabase()
// Setup creates runs and specs in DB
// Teardown cleans up
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute stage integration tests</name>
  <files>tests/conductor/execute-stage.test.ts</files>
  <action>
    Create integration tests for the refactored execute stage. Mock the CLI service (`startExecution`, `getExecution`, `abortExecution`) via `vi.mock` to simulate CLI sessions without actually spawning processes. Use real DB via `getDatabase()` for spec and run state.

    **Test setup pattern:**
    - Create a conductor run in DB via `conductorRepository.createRun()`
    - Create specs in DB via `specRepository.createSpec()` with known affectedFiles
    - Create spec files on disk in temp directory (mock `process.cwd()` or use actual temp path)
    - Mock `cli-service.ts`: `startExecution` returns a fake executionId, `getExecution` returns status after configurable delay

    **Test cases (EXEC-01, EXEC-02):**
    1. "dispatches non-overlapping specs in parallel": Create 2 specs with disjoint files. Assert both `startExecution` calls happen before either completes (check call order).
    2. "serializes overlapping specs": Create 2 specs sharing a file. Assert second `startExecution` only called after first execution completes.
    3. "respects maxConcurrentTasks limit": Create 3 non-overlapping specs with maxConcurrentTasks=2. Assert max 2 concurrent dispatches.

    **Test cases (EXEC-03):**
    4. "updates spec status to executing before dispatch": After execute starts, check `specRepository.getSpecsByRunId()` shows status='executing'.
    5. "updates spec status to completed on success": After execute completes, check status='completed'.
    6. "updates spec status to failed on failure": Mock CLI to return error status, check status='failed'.

    **Test cases (file verification):**
    7. "marks spec failed when CLI exits 0 but no files changed": Mock CLI exit 0, but don't modify any files. Assert spec status='failed' and result includes verification failure reason.
    8. "marks spec completed when CLI exits 0 and files were modified": Mock CLI exit 0, create/touch expected files. Assert spec status='completed'.

    **Mocking strategy:**
    - `vi.mock('@/lib/claude-terminal/cli-service')` for startExecution/getExecution/abortExecution
    - Use `vi.fn()` for onTaskUpdate to track status emissions
    - Create temp directory for projectPath, write actual spec files there
    - For file verification tests: actually create/modify files in temp dir to test real fs checks

    Use `afterEach` to clean up temp dirs and DB records.
  </action>
  <verify>
    <automated>npx vitest run tests/conductor/execute-stage.test.ts -x</automated>
  </verify>
  <done>Execute stage tests pass covering: parallel dispatch of non-overlapping specs, serial dispatch of overlapping specs, maxConcurrentTasks limit, spec status transitions, file verification pass/fail scenarios.</done>
</task>

<task type="auto">
  <name>Task 2: Checkpoint and build validation tests</name>
  <files>tests/conductor/checkpoints.test.ts</files>
  <action>
    Create tests for checkpoint behavior and build validation. These test the orchestrator-level wiring rather than individual modules (which are tested in Plan 01).

    **Approach:** Test checkpoint and build validation logic at the unit level by extracting testable behavior. Since the full orchestrator pipeline loop is complex to test end-to-end (requires mocking all stages), focus on:

    1. **Checkpoint config parsing**: Test that checkpoint config is correctly read from goal records.
    2. **Build validation storage**: Test that `runBuildValidation` result is correctly serialized and stored.
    3. **Checkpoint state transitions**: Test that checkpoint_type is written to DB and cleared on resume.

    **Test cases (EXEC-04):**
    1. "checkpoint config parsed from goal record": Create a goal with `checkpoint_config: JSON.stringify({ triage: false, preExecute: true, postReview: false })`. Read it back and verify the parsed object.
    2. "checkpoint_type written to conductor_runs on pause": Directly call `updateRunInDb(runId, { checkpoint_type: 'pre_execute' })` then read back via `conductorRepository.getRunById()`. Verify the raw DB row has `checkpoint_type = 'pre_execute'`.
    3. "checkpoint_type cleared on resume": Set checkpoint_type, then clear it with `updateRunInDb(runId, { checkpoint_type: null })`. Verify cleared.
    4. "default checkpoint config when goal has no config": Test that missing/null `checkpoint_config` defaults to `{ triage: false, preExecute: false, postReview: false }`.

    **Test cases (VALD-01):**
    5. "build validation result stored on run record": Call `updateRunInDb(runId, { build_validation: JSON.stringify({ passed: true, durationMs: 500 }) })`. Read back via raw SQL and verify JSON parses correctly.
    6. "build validation failure with error output stored": Store a failed result with errorOutput. Read back and verify errorOutput preserved.

    **Setup:**
    - Use real DB via `getDatabase()`
    - Create test runs and goals in DB
    - Import `updateRunInDb` — if it's not exported, use direct DB access with same SQL pattern
    - Clean up in afterEach

    Note: The full orchestrator pause/resume flow is tested implicitly by the existing `state-persistence.test.ts` pattern. These tests focus on the NEW Phase 3 additions (checkpoint_type column and build_validation column).
  </action>
  <verify>
    <automated>npx vitest run tests/conductor/checkpoints.test.ts -x</automated>
  </verify>
  <done>Checkpoint tests pass covering: config parsing from goal, checkpoint_type DB read/write, default config fallback. Build validation tests pass covering: result storage and retrieval, failure output preservation.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/conductor/ --reporter=verbose` — all conductor tests pass (existing + new)
- No regressions in existing tests: `npx vitest run tests/conductor/state-persistence.test.ts tests/conductor/spec-writer.test.ts -x`
</verification>

<success_criteria>
- All new test files pass
- All existing conductor tests still pass
- Test coverage addresses all 5 requirements: EXEC-01 (overlap serialization), EXEC-02 (parallel dispatch), EXEC-03 (status visibility), EXEC-04 (checkpoints), VALD-01 (build validation)
</success_criteria>

<output>
After completion, create `.planning/phases/03-execute-stage/03-03-SUMMARY.md`
</output>
