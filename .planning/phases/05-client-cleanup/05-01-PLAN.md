---
phase: 05-client-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stores/focusModeStore.ts
  - src/stores/terminalStore.ts
  - src/stores/PERSISTENCE_STRATEGY.md
  - src/hooks/useScanData.ts
  - src/hooks/useBacklogQuery.ts
  - src/hooks/useSmoothNavigation.ts
  - src/components/ui/examples/ModalTransitionExample.tsx
  - src/components/ui/buttons/MotionButton.examples.tsx
autonomous: true

must_haves:
  truths:
    - "focusModeStore.ts no longer exists"
    - "terminalStore.ts no longer exists"
    - "All three unused hooks deleted"
    - "Example files deleted"
    - "TypeScript compilation passes"
  artifacts:
    - path: "src/stores/PERSISTENCE_STRATEGY.md"
      provides: "Updated documentation without references to deleted stores"
      contains: "Store Persistence Strategy"
  key_links: []
---

<objective>
Delete unused client-side code: 2 stores, 3 hooks, and 2 example files totaling ~2,395 lines.

Purpose: Reduce maintenance burden by removing dead code identified in v1.1 cleanup analysis
Output: Cleaner codebase with no orphaned client files
</objective>

<execution_context>
@C:\Users\kazda\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\kazda\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete unused stores and update documentation</name>
  <files>
    src/stores/focusModeStore.ts (DELETE)
    src/stores/terminalStore.ts (DELETE)
    src/stores/PERSISTENCE_STRATEGY.md (EDIT)
  </files>
  <action>
    Delete the two unused store files:
    1. Delete `src/stores/focusModeStore.ts` (678 lines)
    2. Delete `src/stores/terminalStore.ts` (428 lines)

    Update `src/stores/PERSISTENCE_STRATEGY.md`:
    - Remove the row for `terminalStore` from the "Always Persist (user_preference)" table (line 34)
    - Remove the row for `focusModeStore` from the "Session Persist (session_work)" table (line 45)

    These stores are NOT exported from any barrel file (index.ts), so no additional cleanup needed.
  </action>
  <verify>
    Run: `npx tsc --noEmit src/stores/*.ts`
    Expected: No errors (remaining stores compile successfully)

    Confirm files deleted:
    - `ls src/stores/focusModeStore.ts` should fail
    - `ls src/stores/terminalStore.ts` should fail
  </verify>
  <done>
    CLIENT-01: focusModeStore.ts deleted
    CLIENT-02: terminalStore.ts deleted
    PERSISTENCE_STRATEGY.md updated (no references to deleted stores)
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete unused hooks and example files</name>
  <files>
    src/hooks/useScanData.ts (DELETE)
    src/hooks/useBacklogQuery.ts (DELETE)
    src/hooks/useSmoothNavigation.ts (DELETE)
    src/components/ui/examples/ModalTransitionExample.tsx (DELETE)
    src/components/ui/buttons/MotionButton.examples.tsx (DELETE)
  </files>
  <action>
    Delete the three unused hooks:
    1. Delete `src/hooks/useScanData.ts` (314 lines)
    2. Delete `src/hooks/useBacklogQuery.ts` (206 lines)
    3. Delete `src/hooks/useSmoothNavigation.ts` (29 lines)

    Delete the two example files:
    4. Delete `src/components/ui/examples/ModalTransitionExample.tsx`
    5. Delete `src/components/ui/buttons/MotionButton.examples.tsx`

    No barrel files export these modules, so no additional cleanup needed.
  </action>
  <verify>
    Run: `npx tsc --noEmit`
    Expected: No errors (full TypeScript compilation passes)

    Confirm files deleted - all should fail:
    - `ls src/hooks/useScanData.ts`
    - `ls src/hooks/useBacklogQuery.ts`
    - `ls src/hooks/useSmoothNavigation.ts`
    - `ls src/components/ui/examples/ModalTransitionExample.tsx`
    - `ls src/components/ui/buttons/MotionButton.examples.tsx`
  </verify>
  <done>
    CLIENT-03: useScanData.ts deleted
    CLIENT-04: useBacklogQuery.ts deleted
    CLIENT-05: useSmoothNavigation.ts deleted
    CLIENT-06: Example files deleted (ModalTransitionExample.tsx, MotionButton.examples.tsx)
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Full TypeScript check:
   ```bash
   npx tsc --noEmit
   ```
   Expected: Exit code 0, no errors

2. Verify deleted file count:
   - 2 store files deleted (~1,106 lines)
   - 3 hook files deleted (~549 lines)
   - 2 example files deleted (~740 lines)
   - Total: 7 files, ~2,395 lines removed

3. Grep confirmation (should return 0 results for imports):
   ```bash
   grep -r "focusModeStore\|terminalStore\|useScanData\|useBacklogQuery\|useSmoothNavigation" src/ --include="*.ts" --include="*.tsx" | grep -v "PERSISTENCE_STRATEGY"
   ```
</verification>

<success_criteria>
- [ ] All 7 files deleted
- [ ] PERSISTENCE_STRATEGY.md updated to remove deleted store references
- [ ] `npx tsc --noEmit` passes with zero errors
- [ ] No import errors anywhere in codebase
- [ ] Requirements CLIENT-01 through CLIENT-06 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/05-client-cleanup/05-01-SUMMARY.md`
</output>
