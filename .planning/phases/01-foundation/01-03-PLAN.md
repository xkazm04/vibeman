---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/app/api/conductor/run/route.ts
  - src/app/api/conductor/status/route.ts
  - src/app/api/conductor/history/route.ts
autonomous: false
requirements: [FOUND-01, FOUND-03, GOAL-01]

must_haves:
  truths:
    - "POST /api/conductor/run starts a pipeline run linked to a goal and returns the run ID"
    - "GET /api/conductor/status?runId=X returns DB-persisted run state including stage details"
    - "GET /api/conductor/history?projectId=X returns past runs with metrics and duration"
    - "User can verify in the UI that a started run shows correct status after page refresh"
  artifacts:
    - path: "src/app/api/conductor/run/route.ts"
      provides: "POST endpoint to start pipeline run with goalId"
      exports: ["POST"]
    - path: "src/app/api/conductor/status/route.ts"
      provides: "GET endpoint returning DB-persisted run state"
      exports: ["GET"]
    - path: "src/app/api/conductor/history/route.ts"
      provides: "GET endpoint for queryable run history"
      exports: ["GET"]
  key_links:
    - from: "src/app/api/conductor/run/route.ts"
      to: "conductorOrchestrator"
      via: "import startRun"
      pattern: "startRun"
    - from: "src/app/api/conductor/status/route.ts"
      to: "conductor.repository.ts"
      via: "import conductorRepository"
      pattern: "conductorRepository\\.getRunById"
    - from: "src/app/api/conductor/history/route.ts"
      to: "conductor.repository.ts"
      via: "import conductorRepository"
      pattern: "conductorRepository\\.getRunHistory"
---

<objective>
Update the API routes to use the new DB-backed conductor repository and orchestrator, then verify the full integration with a human checkpoint.

Purpose: The API routes are the bridge between UI and the new DB-first pipeline. Without updating them, the UI would still try to use the old globalThis-based state. This plan completes the foundation by wiring everything together and verifying end-to-end.

Output: Updated API routes, human-verified integration.
</objective>

<execution_context>
@C:/Users/kazim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kazim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

@src/app/api/conductor/run/route.ts
@src/app/api/conductor/status/route.ts
@src/app/api/conductor/history/route.ts
@src/app/features/Manager/lib/conductor/conductor.repository.ts
@src/app/features/Manager/lib/conductor/conductorOrchestrator.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update API routes to use DB-backed conductor</name>
  <files>src/app/api/conductor/run/route.ts, src/app/api/conductor/status/route.ts, src/app/api/conductor/history/route.ts</files>
  <action>
1. Update `src/app/api/conductor/run/route.ts`:
   - POST handler accepts `{ projectId, goalId, config? }` in request body
   - Calls the rewritten orchestrator's startRun with goalId parameter
   - Returns `{ runId, status: 'running' }` as JSON response
   - Error handling: 400 for missing projectId/goalId, 500 for orchestrator errors

2. Update `src/app/api/conductor/status/route.ts`:
   - GET handler reads `runId` from searchParams
   - Calls `conductorRepository.getRunById(runId)` -- reads from DB, not globalThis
   - Returns full run state including stages, metrics, currentStage, status
   - Returns 404 if run not found

3. Update `src/app/api/conductor/history/route.ts`:
   - GET handler reads `projectId` and optional `limit` from searchParams
   - Calls `conductorRepository.getRunHistory(projectId, limit)`
   - Returns array of run summaries with metrics and duration
   - Returns 400 if projectId missing

Keep existing route patterns (NextResponse.json, try/catch). Do not change URL structure -- these are existing routes being updated, not new routes.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && npx vitest run tests/conductor/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All three API routes use conductorRepository for state. No globalThis references remain in any conductor API route. TypeScript compiles clean. All tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Phase 1 Foundation end-to-end</name>
  <action>Human verifies the complete Phase 1 Foundation integration: DB-persisted conductor pipeline state, typed stage contracts, structured goal input, queryable run history.</action>
  <what-built>Complete Phase 1 Foundation: DB-persisted conductor pipeline state, typed stage contracts, structured goal input, queryable run history. The conductor orchestrator now uses SQLite instead of globalThis for all state management.</what-built>
  <how-to-verify>
    1. Run the full test suite: `npx vitest run tests/conductor/ --reporter=verbose`
    2. Verify TypeScript compiles: `npx tsc --noEmit`
    3. Start dev server: `npm run dev`
    4. Verify the conductor status endpoint returns DB state: `curl http://localhost:3000/api/conductor/history?projectId=test`
    5. (Optional) If a project exists, try starting a conductor run and verify the status endpoint reflects the DB-persisted state
    6. Verify no globalThis state references remain: search for `globalForConductor` in conductor files -- should return zero results except possibly in comments
  </how-to-verify>
  <verify>Human approval</verify>
  <done>User confirms the foundation is working: tests pass, types compile, API routes return DB state, no globalThis leaks.</done>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npx vitest run` passes (full suite, no regressions)
- API routes return DB-persisted state
- No globalThis references for run state in conductor code
</verification>

<success_criteria>
1. Pipeline run started before restart is visible with correct status after restart (FOUND-01 success criterion)
2. Stage functions have typed I/O contracts enforced at compile time (FOUND-02)
3. Pipeline run history is queryable with stage logs, duration, and status (FOUND-03)
4. User can define structured goal with constraints (GOAL-01)
5. Human verifies end-to-end integration works
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
