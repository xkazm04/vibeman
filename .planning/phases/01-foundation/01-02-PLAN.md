---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/features/Manager/lib/conductor/conductor.repository.ts
  - src/app/features/Manager/lib/conductor/conductorOrchestrator.ts
  - src/app/db/repositories/goal.repository.ts
autonomous: true
requirements: [FOUND-01, FOUND-02, FOUND-03, GOAL-01]

must_haves:
  truths:
    - "A pipeline run created via conductorRepository persists in SQLite and is queryable"
    - "Stage completion writes to DB atomically before orchestrator advances"
    - "Interrupted runs are detected and marked on startup recovery"
    - "Goal with constraint fields can be created and retrieved via goal repository"
    - "Run history returns past runs with stage logs, duration, and status"
  artifacts:
    - path: "src/app/features/Manager/lib/conductor/conductor.repository.ts"
      provides: "CRUD for pipeline runs, stage persistence, history queries, recovery"
      exports: ["conductorRepository"]
    - path: "src/app/features/Manager/lib/conductor/conductorOrchestrator.ts"
      provides: "DB-first orchestrator replacing globalThis state"
      contains: "executeStageAndPersist"
    - path: "src/app/db/repositories/goal.repository.ts"
      provides: "Extended goal CRUD with constraint fields"
      contains: "target_paths"
  key_links:
    - from: "conductor.repository.ts"
      to: "conductor_runs table"
      via: "getDatabase() SQL queries"
      pattern: "conductor_runs"
    - from: "conductorOrchestrator.ts"
      to: "conductor.repository.ts"
      via: "conductorRepository import"
      pattern: "conductorRepository\\."
    - from: "goal.repository.ts"
      to: "goals table"
      via: "getDatabase() SQL queries"
      pattern: "target_paths|checkpoint_config"
---

<objective>
Build the conductor repository (DB access layer), rewrite the orchestrator to use DB-first state instead of globalThis, and extend the goal repository with constraint fields.

Purpose: This is the core implementation plan. The repository provides atomic stage persistence. The orchestrator uses it to survive restarts. The goal repository accepts structured goal input. Together these satisfy all four phase requirements.

Output: Working conductor.repository.ts, rewritten orchestrator, extended goal.repository.ts. All Wave 0 tests should pass.
</objective>

<execution_context>
@C:/Users/kazim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kazim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

@src/app/features/Manager/lib/conductor/types.ts
@src/app/features/Manager/lib/conductor/conductorOrchestrator.ts
@src/app/db/repositories/goal.repository.ts
@src/app/db/repositories/generic.repository.ts
@src/app/db/connection.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build conductor.repository.ts with full pipeline CRUD</name>
  <files>src/app/features/Manager/lib/conductor/conductor.repository.ts, tests/conductor/state-persistence.test.ts, tests/conductor/run-history.test.ts</files>
  <behavior>
    - createRun() inserts a new row with status='running', empty stages_state, linked goalId
    - completeStage() atomically updates stages_state JSON and current_stage column
    - getRunById() returns full PipelineRun with parsed JSON fields
    - getRunHistory() returns runs ordered by started_at DESC with configurable limit
    - markInterruptedRuns() updates all 'running'/'paused' runs to 'interrupted'
    - updateRunStatus() changes status and optionally sets completed_at
    - checkAbort() reads should_abort flag for a run
    - setAbort() sets should_abort=1 for a run
  </behavior>
  <action>
Create `src/app/features/Manager/lib/conductor/conductor.repository.ts`:

1. Import `getDatabase` from `@/app/db/connection` and types from `./types`.

2. Implement `conductorRepository` object (not class, matches project pattern) with methods:
   - `createRun({ id, projectId, goalId, config })` -- INSERT into conductor_runs with JSON.stringify for config_snapshot, stages_state (empty stages map), metrics
   - `completeStage(runId, stage, stageState)` -- Read current stages_state, update the stage entry, write back. Use `db.transaction()` to prevent partial writes. Also update current_stage column.
   - `updateRunStatus(runId, status, metrics?)` -- UPDATE status, optionally completed_at and metrics
   - `getRunById(runId)` -- SELECT with JSON.parse for stages_state, metrics, config_snapshot
   - `getRunHistory(projectId, limit=20)` -- SELECT ordered by started_at DESC, parse JSON fields
   - `markInterruptedRuns()` -- UPDATE WHERE status IN ('running', 'paused') SET status='interrupted'. Guard with globalThis.__conductorRecoveryDone boolean to prevent HMR re-triggering.
   - `checkAbort(runId)` -- SELECT should_abort WHERE id=?
   - `setAbort(runId)` -- UPDATE should_abort=1 WHERE id=?

3. Update the Wave 0 test stubs in `tests/conductor/state-persistence.test.ts` and `tests/conductor/run-history.test.ts`:
   - Remove skip/todo markers
   - Import conductorRepository
   - In beforeAll: call getDatabase() and run migrations to ensure schema exists
   - Write real test implementations per the stub descriptions from Plan 01
   - Tests must pass green

Use the existing pattern from `goal.repository.ts` for style guidance. All DB operations use `getDatabase()` synchronous API.
  </action>
  <verify>
    <automated>npx vitest run tests/conductor/state-persistence.test.ts tests/conductor/run-history.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>conductorRepository exported with all methods. state-persistence and run-history tests pass green. Stage completion persists atomically. History queries return correct order and respect limit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend goal.repository.ts and rewrite orchestrator for DB-first state</name>
  <files>src/app/db/repositories/goal.repository.ts, src/app/features/Manager/lib/conductor/conductorOrchestrator.ts, tests/conductor/goal-input.test.ts</files>
  <behavior>
    - Goal create/update accepts constraint fields (targetPaths, excludedPaths, maxSessions, priority, checkpointConfig, useBrain)
    - Goal retrieval returns parsed constraint fields
    - Orchestrator startRun() creates DB run via conductorRepository, not globalThis
    - Orchestrator stage loop calls executeStageAndPersist which writes to DB before advancing
    - Orchestrator checks should_abort between stages
    - On module load, markInterruptedRuns() is called (with globalThis guard)
  </behavior>
  <action>
1. Extend `src/app/db/repositories/goal.repository.ts`:
   - Add constraint fields to create/update functions: target_paths (JSON), excluded_paths (JSON), max_sessions, priority, checkpoint_config (JSON), use_brain
   - Add parsing in read functions: JSON.parse for array/object fields, defaults for missing values
   - Keep all existing functionality intact -- additive changes only
   - Update the goal-input.test.ts stubs to real tests and verify they pass

2. Rewrite `src/app/features/Manager/lib/conductor/conductorOrchestrator.ts`:
   - Remove `globalForConductor` and `conductorActiveRuns` Map
   - Import conductorRepository
   - `startRun(projectId, goalId, config)`: Create run via conductorRepository.createRun(), then enter stage loop
   - Stage loop: For each stage in PIPELINE_STAGES, call `executeStageAndPersist(runId, stage, input)` which:
     a. Calls conductorRepository.completeStage() to mark 'running'
     b. Executes the stage function (keep existing stage dispatch logic)
     c. Calls conductorRepository.completeStage() with result
     d. Checks conductorRepository.checkAbort() -- if true, update status to 'interrupted' and return
   - `stopRun(runId)`: Call conductorRepository.setAbort(runId) instead of abortController.abort()
   - `getRunStatus(runId)`: Read from conductorRepository.getRunById() instead of globalThis Map
   - On module load: Call `conductorRepository.markInterruptedRuns()` with globalThis guard
   - Keep the existing stage execution functions (scout, triage, batch, execute, review) -- just change how they're dispatched and how state is persisted
   - Keep existing error handling patterns but persist errors to DB via updateRunStatus

Important: The stage functions themselves are NOT rewritten in this phase. Only the orchestration wrapper (state management, stage dispatch, persistence) changes. Stage implementations remain as-is.
  </action>
  <verify>
    <automated>npx vitest run tests/conductor/ --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <done>Goal repository accepts and returns constraint fields. Orchestrator uses DB-first state with no globalThis for run storage. All Wave 0 tests pass. goal-input tests pass green.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npx vitest run tests/conductor/ --reporter=verbose` -- all tests green
- `npx vitest run tests/api/conductor/pipeline.test.ts` -- no regression
- Manual check: conductorOrchestrator.ts has no `globalForConductor` references
</verification>

<success_criteria>
1. conductorRepository provides atomic stage persistence with transaction safety
2. Orchestrator creates and manages runs via DB, not globalThis
3. Interrupted run recovery works on startup (with HMR guard)
4. Goal repository accepts all structured constraint fields
5. All four Wave 0 test files pass green
6. No regressions in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
