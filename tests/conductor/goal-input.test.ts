/**
 * Goal Input Tests (GOAL-01)
 *
 * Validates that goals can be created with all constraint fields
 * and that defaults are applied correctly.
 *
 * These are stubs pending conductorRepository implementation in Plan 02.
 */

import { describe, test } from 'vitest';

describe('goal input (GOAL-01)', () => {
  test.todo('creates goal with all constraint fields');

  test.todo('constraint fields default correctly when omitted');

  test.todo('checkpoint_config serializes and deserializes as JSON');
});
