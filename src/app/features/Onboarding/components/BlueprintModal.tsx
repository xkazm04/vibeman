'use client';

/**
 * BlueprintModal - Blueprint feature removed (deprecated)
 * Keeping as stub to avoid breaking imports.
 */
export default function BlueprintModal({ isOpen, onClose }: { isOpen: boolean; onClose: () => void }) {
  return null;
}
