import type { BrainEvent } from './types';

/**
 * Stub – mock data removed. The canvas now relies on real signals
 * from the behavioral_signals table via /api/brain/signals.
 */
export function generateEvents(): BrainEvent[] {
  return [];
}
