'use client';

/**
 * EventsBarChart - Blueprint events feature removed (deprecated)
 * This component previously displayed blueprint scan event statistics.
 * Keeping as stub to avoid breaking imports.
 */
export default function EventsBarChart({ projectId }: { projectId: string; limit?: number }) {
  return null;
}
