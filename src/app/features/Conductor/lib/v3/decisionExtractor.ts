/**
 * Decision Extractor — re-exports from reportGenerator
 *
 * Thin wrapper providing the extractDecisions function and PipelineDecision type.
 */

export { extractDecisions, type PipelineDecision } from './reportGenerator';
