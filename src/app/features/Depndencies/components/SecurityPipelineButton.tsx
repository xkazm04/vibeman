'use client';

/**
 * SecurityPipelineButton - Security patch feature removed (deprecated)
 * Keeping as stub to avoid breaking DependenciesTab imports.
 */
export default function SecurityPipelineButton(_props: { projectId: string; projectPath: string; projectType?: string }) {
  return null;
}
